import React, { useEffect, useRef } from 'react'
import * as fabric from 'fabric';

const SelectionTool = () => {
    const selection = new fabric.SprayBrush(canvas)
  return (
    <div>
      
    </div>
  )
}

export default SelectionTool
